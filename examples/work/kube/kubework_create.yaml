# This work is used to create nginx deployment to clusters cluster0
apiVersion: mcm.ibm.com/v1alpha1
kind: Work
metadata:
  name: nginx-work-create
  namespace: cluster0
spec:
  cluster:
    name: cluster0
  type: Action
  actionType: Create
  kube:
    resource: deployment
    namespace: default
    template:
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: nginx-deployment
      spec:
        selector:
          matchLabels:
            app: nginx
        replicas: 2
        template:
          metadata:
            labels:
              app: nginx
          spec:
            containers:
            - name: nginx
              image: nginx:1.7.9
              ports:
              - containerPort: 80
